/**
 * Routes d'authentification — **Vague 1**.
 *
 * Emplacement réservé : rien n'est implémenté ici en Vague 0.
 *
 * Contenu prévu (voir CLAUDE.md, Vague 1) :
 *   POST /api/auth/login             connexion (compte ACTIF uniquement)
 *   POST /api/auth/refresh           renouvellement du jeton d'accès
 *   POST /api/auth/logout            révocation du jeton de rafraîchissement
 *   POST /api/auth/change-password   changement de mot de passe (obligatoire
 *                                    à la première connexion)
 *
 * Ces routes devront être protégées par `authRateLimiter`
 * (`middleware/rate-limit.ts`) et valider chaque entrée avec `validate()`.
 */

export {};
